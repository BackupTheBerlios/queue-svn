indexing
	description: "Captures the variables of an object that change from one position update to the next"
	author: "Andreas Kaegi"

class
	Q_PHYS_STATE
	
feature -- interface

	apply_to (o: Q_OBJECT) is
			-- Assign variable values to object
		do
		end

end -- class Q_PHYS_STATE
